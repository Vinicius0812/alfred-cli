version: 1

project:
  name: example-php-api

commit:
  convention: conventional
  allowed_types: [feat, fix, docs, refactor, test, chore]
  scopes: [api, domain, database, infrastructure]
  protected_branches: [main]
  confirm_push: true

docker:
  compose_files:
    - compose.yaml
  env_files:
    - .env

workflows:
  setup:
    description: Prepare application dependencies
    steps:
      - command: composer
        args: [install]
        risk: network
      - command: php
        args: [artisan, migrate]
        confirm: true
        risk: local-write

  test:
    description: Run the PHP test suite
    steps:
      - command: composer
        args: [test]
        risk: read

policies:
  confirm_destructive_actions: true
